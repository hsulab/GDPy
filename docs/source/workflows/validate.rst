Validate
========

**Validate* operations requires `validator`, `structures` (dataset), and `worker` (optional) as 
inputs.

.. code-block:: yaml

    variables:
        ...
    operations:
        ...
    sessions:
        ...
