Validate
========

**Validate*8 operations requires `validator`, `structures` (dataset), and `worker` (optional) as 
inputs.

.. code-block:: yaml

    variables:
        ...
    operations:
        ...
    sessions:
        ...
